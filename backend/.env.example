PORT=
DATABASE_URL=
JWT_SECRET=
JWT_REFRESH_SECRET=

# Optional: password for seeded admin@verdi.com (default only if unset — change after login)
# ADMIN_SEED_PASSWORD=

# Optional (defaults shown in src/config/index.js if unset)
# NODE_ENV=development
# JWT_ACCESS_EXPIRES_IN=15m
# JWT_REFRESH_EXPIRES_IN=7d
# CORS_ORIGIN=http://localhost:3000,http://localhost:5173
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX=200
# BCRYPT_ROUNDS=12
